apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: default-priority
value: 100
globalDefault: true  # This makes it the cluster-wide default
description: "Default priority class for all pods without a specified priority class."